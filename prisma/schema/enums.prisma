enum Role {
  USER
  LANDLORD
  ADMIN
}

enum AuthProvider {
  CREDENTIALS
  GOOGLE
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum PropertyType {
  APARTMENT
  HOUSE
  SUBLET
  HOSTEL
  ROOM
}

enum PropertyStatus {
  PENDING
  APPROVED
  REJECTED
}

enum RoomType {
  SINGLE
  SHEAR
  MASTER
}

enum RoomStatus {
  AVAILABLE
  UNAVAILABLE
  BOOKED
}
