model Amenities {
  id String @id @default(uuid())

  amenityName String @unique

  propertyAmenities PropertyAmenities[]
  roomAmenities     RoomAmenities[]

  createdAt          DateTime     @default(now())
  isDeleted          Boolean      @default(false)
  deletedAt          DateTime?

  @@map("amenities")
}
